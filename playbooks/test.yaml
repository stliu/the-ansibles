- hosts: nginx
  user: root
  gather_facts: no
  vars_files: 
    - default_vars.yaml
    - users.yaml
    - cassandra.yaml
    - directories.yaml
    - jdk.yaml
  vars:
    monit_protection: true
  roles:
     - epel
     - directories
     - hostname
     - timezone
     - base
     - build
     - cron
     - python
     - jdk
     - scala
     - supervisor
     - monit
     - nginx
     - cassandra
     - zookeeper
     - kafka
